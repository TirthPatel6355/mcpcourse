[project]
name = "mcpcourse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tirth Patel", email = "tirthk1510@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=2.2.0",
]

[project.scripts]
mcpcourse = "mcpcourse:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
